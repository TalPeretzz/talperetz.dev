/**
 * Short entry paths for share links (each becomes its own path in analytics).
 *
 * - in — LinkedIn
 * - tw — Twitter / X
 * - gh — GitHub
 * - nl — Newsletter / email
 */
export const REFERRAL_SLUGS = ["in", "tw", "gh", "nl"] as const;
